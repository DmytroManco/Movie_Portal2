import { MovieDetailComponent } from './movie_detail.component';

describe('MovieDetailComponent', () => {
  let sut: any;

  beforeEach(() => {
    // sut = new MovieDetailComponent();
  });

  xit('Should match interface', () => {

  });
});
